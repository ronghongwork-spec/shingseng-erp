# 複製這個檔案為 .env（本機開發用），並填入實際值
# .env 本身不會、也不應該被上傳到 GitHub（已在 .gitignore 排除）
#
# 部署到 Render 時，不需要 .env 檔，
# 而是到 Render 後台 → Environment → Add Environment Variable 逐一新增下面三個 key。

A1_API_KEY=YOUR_A1_API_KEY
A1_API_PASSWORD=YOUR_A1_API_PASSWORD
A1_COMPANY_NAME=海濤客食品廠2
